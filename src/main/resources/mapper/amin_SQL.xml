<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" " http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="amin">

<!-- 민재영 - amin  -->

	<!-- 페이징  -->
	<select id="member" parameterType="hashmap" resultType="hashmap">
		<include refid="common.pagingPre" />
		<![CDATA[
	SELECT
			ROW_NUMBER() OVER (ORDER BY USER_NUM DESC) RNUM,
			USER_NUM, USER_ID, USER_NIC, USER_PW, USER_BLIND, USER_EVENTAPPLY, AMIN_TIM, DEL_GB, USER_EVENTWIN
		FROM (SELECT ROW_NUMBER() OVER (ORDER BY USER_NUM DESC) RNUM,
			USER_NUM, USER_ID, USER_NIC, USER_PW, USER_ORIGINAL, USER_NEW_IMG, USER_FILE_SIZE, USER_BLIND, USER_EVENTAPPLY, AMIN_TIM, DEL_GB, USER_EVENTWIN
            		FROM TB_MEMBER)
		]]>
		<include refid="common.pagingPost" />
	</select>
	
	<!-- keyword -->
	<select id="search" parameterType="hashmap" resultType="hashmap">
		<![CDATA[
		SELECT
			ROW_NUMBER() OVER (ORDER BY USER_NUM DESC) RNUM,
			USER_NUM, USER_ID, USER_NIC, USER_PW, USER_BLIND, USER_EVENTAPPLY, AMIN_TIM, DEL_GB, USER_EVENTWIN
		FROM (SELECT ROW_NUMBER() OVER (ORDER BY USER_NUM DESC) RNUM,
			USER_NUM, USER_ID, USER_NIC, USER_PW, USER_ORIGINAL, USER_NEW_IMG, USER_FILE_SIZE, USER_BLIND, USER_EVENTAPPLY, AMIN_TIM, DEL_GB, USER_EVENTWIN
		FROM TB_MEMBER) where USER_ID LIKE '%${keyword}%'
		]]>
	</select>



	<!-- <insert id="memberup" parameterType="hashmap">
		<![CDATA[
		UPDATE TB_NOTICE SET 
		NOTICE_TITLE = #{NOTICE_TITLE}, 
		NOTICE_CONTENT = #{NOTICE_CONTENT}, 
		NOTICE_ORIGINAL = #{NOTICE_ORIGINAL}, 
		NOTICE_NEW_IMG = #{NOTICE_NEW_IMG}, 
		NOTICE_FILE_SIZE = #{NOTICE_FILE_SIZE}
		WHERE
		NOTICE_INDEX = #{NOTICE_INDEX}
		]]>
	</insert>
 -->
	
	
	
	<!--
	<insert id="noticeUp" parameterType="hashmap">
		<![CDATA[
		UPDATE TB_NOTICE SET 
		NOTICE_TITLE = #{NOTICE_TITLE}, 
		NOTICE_CONTENT = #{NOTICE_CONTENT}, 
		NOTICE_ORIGINAL = #{NOTICE_ORIGINAL}, 
		NOTICE_NEW_IMG = #{NOTICE_NEW_IMG}, 
		NOTICE_FILE_SIZE = #{NOTICE_FILE_SIZE}
		WHERE
		NOTICE_INDEX = #{NOTICE_INDEX}
		]]>
	</insert>
	 -->
	<!--
	<insert id="noticeDe" parameterType="hashmap">
		<![CDATA[
		UPDATE TB_NOTICE SET 
		NOTICE_KATE = 'Y'
		WHERE NOTICE_INDEX = #{NOTICE_INDEX}
		]]>
	</insert>
	-->
</mapper>